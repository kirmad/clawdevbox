{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "dev-buddy",
  "version": "1.3.0",
  "description": "Senior-engineer main agent for clawdevbox. Long-lived, autonomous, and independent — plans, delegates heavy work to background sub-agents or forked `session.send` workspaces (so the chat loop stays responsive), verifies, surfaces results via the inbox, recalls and writes durable memory, and continually proposes new tools/skills/recipes to make the user's workflow smoother.",
  "author": { "name": "Clawdevbox team" },
  "keywords": ["clawdevbox", "main-agent", "autonomous", "developer-assistant", "senior-engineer"],
  "clawdevbox": {
    "recipes": [
      { "id": "heartbeat-pulse", "file": "recipes/heartbeat-pulse.yaml" },
      { "id": "daily-standup", "file": "recipes/daily-standup.yaml" }
    ]
  }
}
