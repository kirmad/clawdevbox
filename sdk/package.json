{
  "name": "@conductor/sdk",
  "version": "1.0.0",
  "description": "Type-only SDK for Conductor plugin authors. Exports the ToolContext interface that hostable tools (`<plugin>/tools/*.ts`) receive as their second argument when Conductor's MCP server invokes them. See spec §10.3 for the hostable-tool contract; this package is intentionally zero-runtime — every export is a TypeScript type or interface.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "license": "MIT",
  "keywords": [
    "conductor",
    "plugin",
    "mcp",
    "sdk",
    "types"
  ]
}
